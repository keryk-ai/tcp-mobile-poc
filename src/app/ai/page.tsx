'use client';

import AppShell from '@/components/AppShell';

// Placeholder chrome — full voice-screen restyle lands in a later task.
export default function AIPage() {
  return (
    <AppShell>
      <div className="flex-1 flex items-center justify-center p-4">
        <p className="text-gray-400 text-sm">AI assistant coming soon.</p>
      </div>
    </AppShell>
  );
}
